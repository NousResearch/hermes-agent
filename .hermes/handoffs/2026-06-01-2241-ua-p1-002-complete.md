# Handoff: UA-P1-002 Bundle Manifest and Target Cleanliness Hardening

## Context
- Bead executed: `/home/jarrad/work/plans/ua-phase1-execution/.beads/ua-p1-002-bundle-cleanliness-hardening.md`.
- Target repo: `/home/jarrad/.hermes/hermes-agent`.
- Branch: `feat/ua-001-run-bundle`.
- Authority: implementation and verification only; no commit/push/merge/deploy authority.
- Required gates: coder implementation, Hermes-owned focused/full verification, reviewer PASS.
- Known out-of-scope dirty files preserved/excluded:
  - `tests/tools/test_skills_sync.py`
  - `tools/skills_sync.py`

## Work Completed
- Implemented manifest target-cleanliness tracking in:
  - `scripts/code-scan/run_bundle.py`
  - `scripts/code-scan/run_ua.py`
- Added/updated focused tests in:
  - `tests/code_scan/test_run_bundle.py`
  - `tests/code_scan/test_run_ua.py`
- Successful manifests now include `status: "complete"`.
- Failure manifests are written on pipeline exceptions after bundle creation with:
  - `status: "failed"`
  - `failure_stage`
  - `error_message`
  - artifact paths already registered
  - target cleanliness fields when available
- Manifests now include:
  - `target_mutation_allowed`
  - `target_dirty_before`
  - `target_dirty_after`
  - `target_dirty_files_before`
  - `target_dirty_files_after`
  - `unexpected_target_changes`
  - `target_cleanliness_status`
- Default external-cache scans are tested not to create target-local:
  - `.hermes/code-state`
  - `.hermes/code-scan-cache`
  - `.ua`

## Verification

### Coder-reported RED evidence
Focused new tests failed before implementation with 14 failures, including missing manifest cleanliness fields and missing `status: complete` assertions:

```text
FAILED tests/code_scan/test_run_bundle.py::TestManifestCleanlinessFields::test_manifest_has_cleanliness_field[target_dirty_before]
FAILED tests/code_scan/test_run_bundle.py::TestManifestCleanlinessFields::test_manifest_has_cleanliness_field[target_dirty_after]
FAILED tests/code_scan/test_run_bundle.py::TestManifestCleanlinessFields::test_manifest_has_cleanliness_field[target_dirty_files_before]
FAILED tests/code_scan/test_run_bundle.py::TestManifestCleanlinessFields::test_manifest_has_cleanliness_field[target_dirty_files_after]
FAILED tests/code_scan/test_run_bundle.py::TestManifestCleanlinessFields::test_manifest_has_cleanliness_field[unexpected_target_changes]
FAILED tests/code_scan/test_run_bundle.py::TestManifestCleanlinessFields::test_manifest_has_cleanliness_field[target_cleanliness_status]
FAILED tests/code_scan/test_run_bundle.py::TestSuccessfulManifestStatus::test_successful_run_has_status_complete
FAILED tests/code_scan/test_run_ua.py::TestUAManifestCleanliness::test_successful_ua_has_status_complete
14 failed in 3.16s
```

### Hermes-owned focused verification

```bash
cd /home/jarrad/.hermes/hermes-agent
python -m pytest tests/code_scan/test_run_bundle.py tests/code_scan/test_run_ua.py -q
```

Result: PASS.

```text
80 passed in 24.17s
```

### Hermes-owned full code-scan verification

```bash
cd /home/jarrad/.hermes/hermes-agent
python -m pytest tests/code_scan -q
```

Result: PASS.

```text
480 passed in 83.50s (0:01:23)
```

### Diff/scope verification

```bash
git diff --check -- scripts/code-scan/run_bundle.py scripts/code-scan/run_ua.py tests/code_scan/test_run_bundle.py tests/code_scan/test_run_ua.py
```

Result: PASS.

```text
PASS scoped diff --check
```

```bash
python -m py_compile scripts/code-scan/run_bundle.py scripts/code-scan/run_ua.py
```

Result: PASS.

```text
PASS py_compile
```

Diff artifact:

```text
/tmp/ua-p1-002-diff.patch — 769 lines / 34690 bytes
```

Scope check output contained only the two known pre-existing dirty files outside the four in-scope UA files:

```text
tests/tools/test_skills_sync.py
tools/skills_sync.py
```

## Reviewer Verdict
- Reviewer result: PASS.
- Reviewer checked spec compliance, read-only target semantics, partial-failure behavior, test adequacy, and scope preservation.
- Non-blocking notes:
  - Local `subprocess` import style in `run_ua.py` is acceptable but could be cleaner.
  - Delta-mode `_current_stage` can report `summary` rather than `manifest` for some manifest-stage failures; `error_message` still carries the real cause.
  - `run_ua.py` tests do not include the exact dirty-target test that `run_bundle.py` has; accepted because run_bundle covers it end-to-end and run_ua shares the same pattern.

## Subagent Reliability
- Coder subagent completed and returned exact RED/GREEN/FULL evidence.
- Hermes independently reran focused and full tests.
- Reviewer subagent completed and returned PASS.
- Expected vs actual artifacts: matched; four in-scope files changed and no commit/push performed.

## Issues / Caveats
- Work is complete locally but not committed or pushed.
- Existing unrelated dirty files remain out of scope and local:
  - `tests/tools/test_skills_sync.py`
  - `tools/skills_sync.py`
- No merge, deploy, or production mutation performed.

## Next Recommended Action
- If JC approves, perform a narrow commit/push for UA-P1-002 only on `feat/ua-001-run-bundle`, staging only the four implementation/test files plus this handoff and `.hermes/PROJECT_STATE.md`; continue excluding the two unrelated dirty files.
