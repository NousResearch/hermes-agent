- 🧠 **Memory cap while saving xAI note** — `memory.add` failed at 2,182/2,200 chars; fixed by replacing the compact Hermes entry with `xAI OAuth+x_search verified`.
- 🏷️ **WMS backfill worker stalled then exited 143** — First broad Codex worker (`proc_2e506c617bc5`) looped on repository-wide cost searches and produced no useful diff; kill it, write a failed summary, relaunch with a focused implementation-only/cleanup spec, then host-verify and commit the narrowed result.
- 🏷️ **Piped `gh pr view` into heredoc Python incorrectly** — `gh pr view ... | python3 - <<'PY'` made Python read the heredoc instead of gh JSON, causing `JSONDecodeError`; write JSON to a temp file or use `python3 -c` when parsing piped gh output.
- 🏷️ **Backticks inside double-quoted `run-agent -p` prompt executed in shell** — A WMS Codex prompt containing markdown backticks caused `bash: .ts: command not found` and related errors before the worker could start; write complex prompts to a spec file and pass `--spec` instead of inline `-p`.
- 🏷️ **`gh pr merge --delete-branch` can merge before local checkout cleanup fails** — In WMS worktrees, `gh pr merge --squash --delete-branch` reported `fatal: 'main' is already used by worktree` after merging PR #5; always verify PR state before retrying to avoid duplicate/confusing actions.
- 🏷️ **Terminal backgrounding guard false-positive on Python bitwise `&`** — A foreground `terminal()` call containing Python/numpy boolean `&` inside a heredoc was rejected as shell backgrounding; use `execute_code()` for Python image/array cleanup snippets or avoid raw `&` in foreground shell heredocs.
- 🏷️ **WMS worker added non-runnable Vitest artifact** — WMS repo has no `vitest` dependency, so a Codex-added `PackingMode.test.ts` was not runnable; remove non-runnable test artifacts during host verification unless an existing harness/package supports them.
- 🏷️ **GitHub implementation watcher has no cheap help mode** — Running `python scripts/github-implementation-watch.py --help` actually entered the watcher and timed out. Inspect the script with `read_file` or run the real watcher deliberately with a long timeout instead.
- 🧠 **Memory cap while adding Uncensored topic mapping** — A full Telegram topic-profile mapping replacement exceeded the 2,200-char memory cap. Fixed by replacing the topic memory with a shorter code-based mapping (`UC12184=personal-uncensored`) and leaving the manifest as source of truth.
- 🏷️ **Hermes-agent origin push permission denied** — Pushing directly to `NousResearch/hermes-agent` returned HTTP 403 for `duclamvan`; push local operational/docs commits to fork remote `duclamvan/hermes-agent` unless an upstream PR is explicitly needed.
- 🏷️ **Piped tool output into heredoc Python incorrectly** — `script | python3 - <<'PY'` made Python read the heredoc script instead of piped JSON, causing `JSONDecodeError`; write output to a temp file or use `python3 -c` for pipe parsing.
- 🏷️ **Skill categories are single directory names** — `skill_manage(action='create', category='mlops/inference')` failed because runtime skill categories cannot include `/`; create under `category='mlops'` or create/write the nested repo path directly when authoring in-repo skills.
- 🏷️ **YouTube captions blocked but local ASR worked** — For video `TS_hH4sdiKs`, `youtube-transcript-api` hit YouTube IP blocking and `yt-dlp --write-auto-sub` hit HTTP 429; fixed by downloading audio with `yt-dlp` and transcribing locally with `whisper-cli -m ~/.cache/whisper-cpp/ggml-base.bin`.
- 🏷️ **Telegram progress send transient ConnectError** — `send_message` to `Hermes - Business / WMS Dev` can fail with `httpx.ConnectError` and blank detail. Immediately retry the same message and include `❌ Failed → ✅ Fixed` so no visible progress failure hangs unresolved.
- 🧠 **Memory cap while adding local Qwen preference** — `memory.add` failed at 2,104/2,200 chars for the Mac mini local-Qwen rule. Fixed by replacing the compact Hermes memory entry to include `hermes-qwen-local` on-demand-only usage and unload-after-use.
- 🏷️ **Pipefail false-negative with `ollama list | grep -q`** — The on-demand Qwen wrapper falsely said the installed model was missing because `grep -q` closed the pipe early under `set -o pipefail`. Fixed by capturing `model_list="$(ollama list)"` and matching with `[[ "$model_list" == *"$MODEL"* ]]`.
- 🏷️ **GitHub polling transient API connection error** — `gh pr view/checks` can exit with `error connecting to api.github.com` mid-poll. Treat as transient, retry the PR polling loop, and report `❌ Failed → ✅ Fixed` if it affected visible progress.
- 🏷️ **Tailscale Serve + Hermes dashboard Host header** — Serving the localhost-bound dashboard directly through Tailscale Serve returns `Invalid Host header` because Serve sends the `.ts.net` Host. Keep dashboard bound to `127.0.0.1:9119`, put Caddy on `:9120` with `header_up Host 127.0.0.1:9119`, then `tailscale serve --bg 9120`.
- 🏷️ **Caddy host-specific block returned empty 200 through Tailscale** — A Caddyfile site label like `127.0.0.1:9120` did not match tailnet Host/IP requests and Tailscale HTTPS returned empty 200 bodies. Use `:9120` as the site label so all tailnet Host headers match, while still rewriting upstream Host for Hermes.
- 🏷️ **Hermes profile context files may be absent in `~/.hermes/hermes-agent`** — `SOUL.md`, `USER.md`, `MEMORY.md`, daily memory files, and `memory/tacit-knowledge.md` can be missing even when AGENTS references them. Treat missing reads as non-blocking, rely on injected profile memory/SESSION-STATE, and avoid repeated read_file retries.
- 🏷️ **WMS worktree rebase with unstaged worker diff** — `git rebase origin/main` fails with unstaged Codex changes. Stash WIP, rebase, then `git stash pop` before host gates.
- 🏷️ **WMS ignored-path tracked file staging** — `git add client/src/hooks/...` can hit an ignored-path guard even for a tracked modified file. Use `git add -u <tracked-file>` before commit; use `git add -f` only if intentionally tracking a new ignored file.
