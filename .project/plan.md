# Plan — Hermes ใช้งานจริงทั้งระบบ (อนุมัติ 2026-07-05 · "ok" ในแชท Fable)

> memory-schema: v1.2 · จาก Use Act-As v3.1 (Fable วิเคราะห์ · เจ้าของอนุมัติ P0-P1 เริ่มทันที · P2-P4 รอปัญหาชุดสุดท้ายก่อนล็อกดีไซน์)
> ทีม: Fable = คิด/ใบสั่ง/ตัดสิน (เฉพาะงานเกรดยาก) · Codex = เขียนโค้ด · Grok = ตรวจ (คนละค่ายคนเขียน) · gate-run = ผู้ตัดสิน verified · เจ้าของ = อนุมัติ/กด merge
> branch งานนี้: `feature/project-os-recovery` · กติกา: งานหลายเฟส = 1 PR เดียว · ห้าม merge→main / deploy เอง (เจ้าของกด)
> แผนเก่า (AI Relay Hardening v2) ปิดไปแล้ว 3/4 เฟส · P4 เดิม (relay-report เงินบาท) ถูกยุบเข้า P5 ของแผนนี้

## P0 — กู้ฐานความจำ (กำลังทำ · แชทนี้)
- P0-I1 กู้ shortcut 3 ตัวที่ถูกถอนกลับ (`use-overviewprogress` / `use-featurespec` / `use-designsystem`) จาก commit `f22b6f3bd` → อัปเข้า Schema v1.2 → ลงคลังกลาง + ทะเบียน + ชุดแจกทีม
- P0-I2 ย้ายความจำ repo นี้เข้า `.project/` (plan/OverviewProgress/decisions) + stub ที่เก่า + แก้จุดอ้าง
- P0-I3 แก้ความจำระบบที่จดผิดว่า "shortcut ครบ 4/4" ให้ตรงของจริง + จดเหตุการณ์ถอนกลับกันซ้ำ
- verify: `git check-ignore` ว่าง + `git ls-files .project/` ครบ + commit SHA + PR เดียว

## P1 — ด่านไฟล์เข้า git จริง เข้ามาตรฐานกลาง (กำลังทำ · แชทนี้)
- P1-I1 เพิ่มด่านใน Memory Schema §1b + Use New Chat + Use Close Chat + Project OS ทั้ง 4 ตัว
- P1-I2 กระจายเข้า `team-shortcuts/payload/` ให้เครื่องพนักงานได้ด้วย
- verify: grep เจอด่านครบทุกไฟล์ + ตัวอย่างจริง repo นี้ผ่านด่าน

## P2 — มาตรฐานกลางวิ่งจริงทั่ว 30-40 โปรเจกต์ (รอปัญหาชุดสุดท้ายจากเจ้าของก่อนล็อก)
- สั่งเครื่องแจก+สแกนความเพี้ยน (`hermes-standard/bin/`: rollout/scan/safe_apply) ทำงานจริง · แจกผ่าน safe_apply เท่านั้น
- รวมมาตรฐาน `.gitignore` กลาง (ทุก project ต้องมีช่องอนุญาต `.project/`) + ไฟล์คุม AI กลาง (CLAUDE/AGENTS/.cursor ฯลฯ) แบบ 2 โซน (กลางแก้จากศูนย์ · โซน project แก้เองได้)
- verify: รายงานความเพี้ยนฉบับแรกครอบ ≥30 โปรเจกต์
- ผู้ทำ: Codex เขียน · Grok ตรวจ

## P3 — ปิดวงจรคำด่า/ความพลาด (รอล็อกพร้อม P2)
- ติดตั้งตัวจับชุดใหม่ (ISSUE-004 ค้าง deploy) + ตัววิเคราะห์ทุก 3 ชม. (คำสั่งติดตั้ง 1 บรรทัดให้เจ้าของ) + ตารางรีวิว (ด่ากี่ครั้ง/หมวด/แก้ยัง/ทำไมซ้ำ) + ด่าซ้ำ→ออกใบงานแก้อัตโนมัติ
- verify: ยิงคำด่าทดสอบ 1 คำ วิ่งครบวงจร จด→วิเคราะห์→ใบงาน

## P4 — ย้ายน้ำหนัก hook จาก "เตือนหลังตอบ" เป็น "กั้นก่อนทำ" (รอล็อกพร้อม P2)
- แปลงกฎที่ละเมิดบ่อยสุดเป็นด่าน PreToolUse + มิเตอร์วัดรายด่าน + ซ่อมตัวนับสถิติเฟ้อ
- verify: มิเตอร์ออกรายงานเทียบก่อน/หลังได้จริง

## P5 — Monitor Hub: URL เดียวเฝ้าทั้งระบบ + การ์ดเช้าเข้า Lark
- หน้าเดียวบน VPS (host เดียวกับเว็บความรู้ `:9722`) 6 ป้าย: คำด่า+สถานะแก้ · violation รายด่าน · ความพลาดของ coder รายตัว (จาก relay ledger) · ค่าใช้จ่าย AI รายวันเป็นบาท (ยุบ P4 เดิมของแผน relay เข้ามาที่นี่) · PR รอเจ้าของ · ผลเฝ้าหลัง deploy
- verify: เปิด URL จริงจากมือถือได้ + การ์ดเข้า Lark จริง 1 ใบ
- ผู้ทำ: Codex เขียน · Grok ตรวจ

## P6 — แผนอัปรุ่น v0.17.0 → v0.18.0 (v2026.7.1) อย่างปลอดภัย
- ปลดล็อกของที่เจ้าของต้องการ: /learn · /journey · memory graph · ตัวพิสูจน์งานด้วยหลักฐาน · MoA
- ครั้งก่อนของต่อเติมหาย 4/7 กลุ่ม → ต้องทำบัญชีของต่อเติม (~3,215 commit) ก่อน + ซ้อมใน worktree `upgrade-audit` แยก
- verify: บัญชีของต่อเติมครบ + ซ้อมอัปผ่านโดยของไม่หาย

## สายคู่ขนาน — JARVIS v2 ผู้ช่วยเสียง (อนุมัติ 2026-07-06 · แชต jarvis · ไม่ปนกับแผนแม่บทนี้)
- แผนตัวจริง: `.project/FeatureSpec-jarvis-voice.md` (plan_id: jarvis-v2-phase-plan · 8 เฟส P0-P7 · ผ่าน cross-check Grok+Codex แก้ครบ 10/10 ข้อ)
- ขอบเขตไฟล์: `scripts/jarvis-voice/` เท่านั้น (แผนแม่บทตั้งกติกา "ห้ามแตะ" โฟลเดอร์นี้ไว้แล้ว — ถูกต้อง เป็นของสาย jarvis)
- เฟสถัดไป: **P0 เจ้าของทดสอบเสียง** → แล้วเปิดแชตใหม่ส่ง Use AI Relay ไล่ P1→P7 (ใบส่งงานก๊อปวางอยู่ท้ายไฟล์แผน)
- คำถามค้างรอเจ้าของ: ข้อมูลชั้นไหนห้ามส่งสมอง cloud — ต้องตอบก่อนเปิดใช้ P7-I2 (ตัวจ่ายงานหลายสมอง)

## งานคน (เจ้าของ)
- กด merge PR แต่ละรอบ (PR #10 relay-p3 ค้างอยู่ + PR ของงานนี้)
- รันคำสั่งติดตั้งตัวตั้งเวลา 1 บรรทัด (P3 · เตรียมให้ก๊อปวาง)
- ส่งปัญหาชุดสุดท้ายก่อนล็อก P2-P4 · ยืนยันเนื้อหา [assumption] ใน BusinessPlan

## ความเสี่ยงค้าง
- แจกมาตรฐานทั้ง 30-40 โปรเจกต์โดยไม่ผ่าน safe_apply = เสี่ยงพังหมู่ (ห้ามข้าม)
- Opus 4.7/4.6 ยังไม่ทดสอบจริง · Grok บน VPS ยังไม่ล็อกอิน
- ตัวนับ violation เฟ้อ — ตัวเลขรายงานเชื่อได้แค่แนวโน้ม จนกว่า P4 จะซ่อม
