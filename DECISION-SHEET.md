# DECISION SHEET — one screen to finalize the v0.17.0 migration scope

Everything mechanical is verified and reproducible (see `PROCEDURE.md` + #50111). The only
thing left is **your scope ratification**. Reply **"accept defaults"** to take every recommended
default below, or override any line. On your reply the run closes with a final coverage re-run.

---

## A. The 25 DISCARD files — accept as out-of-scope for PRs?

Not hand-written source; **0 code references** them (proven by `git grep`). Never contributable.

- **9** `.bak` editor/build backup snapshots (the live file is already in a feature PR)
- **12** `.project-intel/` generated index artifacts (incl. a binary `.sqlite`, regenerated by tooling)
- **4** `transcripts/` eval-capture text files

**▶ Recommended default: ACCEPT as out-of-scope (delete from overlay, do not PR).**
☐ accept   ☐ override: __________

---

## B. The 9 WITHDRAWN files — accept as out-of-scope, with maintainer rationale?

Withdrawn after **maintainer feedback** (not my call alone):

- **agy-cli** (8 files): @teknium1 said it's superseded by the merged native `google-antigravity`
  provider (#50454); then **#50492 removed the whole Google-OAuth provider category for
  account-ban safety** (Google bans the entire account). PRs #50555/#50657 CLOSED.
- **gemini-cli-UA** (`google_user_agent.py`): #50033 spoofed the gemini-cli identity to Google
  OAuth backends — exactly the banned pattern #50492 removed. PR #50033 CLOSED.

**▶ Recommended default: ACCEPT as out-of-scope (maintainer-aligned, ban-risk — do not re-introduce).**
☐ accept   ☐ override: __________

---

## C. The 5 Part-4 decisions — confirm recommended defaults?

| # | Decision | Recommended default |
|---|---|---|
| 1 | #50064 stale-test drop on v0.17.0 (canonical tree itself dropped it) | **ACCEPT the drop** |
| 2 | 40→now 39-PR grouping (one-PR-per-logical-change) | **ACCEPT as-is** |
| 3 | Delivery: per-PR branches + #50111 patch manifest (vs one integration branch) | **ACCEPT per-PR + manifest** |
| 4 | #50626: its `subdirectory_hints.py` half duplicates maintainer-preferred #29433 | **Keep only the xAI-label half** (defer subdir-guard to #29433) |
| 5 | 8 ready-for-review, 31 draft — flip draft→ready? | **Keep current split** (you flip when ready) |

**▶ Recommended default: CONFIRM all 5.**
☐ confirm all   ☐ override #__: __________

---

## What happens on each reply

- **"accept defaults"** → I record A=accept, B=accept, C=confirm-all in `PROCEDURE.md`, apply the
  single follow-on (decision C4: trim #50626 to its xAI-label half), re-run
  `verification/reproduce-coverage.sh` as the closing artifact, and the run is **done**.
- **Any override** → I record it, make the implied change (e.g. if you reject a WITHDRAWN file, I
  build a PR for it; if you keep #50626 whole, no trim), then re-run the coverage script to confirm
  `165 = covered + discard + withdrawn + 0 orphans` still balances.

## Current verified state (unchanged by your decision)

- **39/39 open code PRs stack CLEAN on v0.17.0** (set-level, `stack-apply-v017.sh`); 135 .py compile, 0 fail.
- Coverage: **165 = 131 in PRs + 25 DISCARD + 9 WITHDRAWN + 0 orphans** (`reproduce-coverage.sh`).
- 6 forward-port patches verified apply-clean + tests pass, linked from each PR.
- Latest stable target = **v0.17.0 (`v2026.6.19` / `2bd1977d8`)**, confirmed `isLatest`.
- Canonical `./src` untouched; nothing merged.
