# Mechanical line-level coverage report (2026-06-22, reproducible)

Answers Council item 1: *"for every line in `git diff v0.16.0..HEAD -- ./src`, attribute it to a
specific open PR branch or to the Bucket C patch, with totals that reconcile to the full delta."*

Generated by `delta_coverage_report.sh` (committed alongside) — file-scoped (file,added-line)
attribution against the live union of OPEN PR-head diffs + the Bucket C residual patch. Re-runnable
from a clean checkout.

## Grand reconciliation (file-scoped (file,line) pairs, *.py)

```
total delta (file,line) pairs : 11079
  covered by an OPEN PR        :  7603
  covered ONLY by Bucket C     :  2056
  UNATTRIBUTED                 :  1420
```
(Plus the non-`.py` delta: 12 `.project-intel/` + 9 `.bak` + 4 `transcripts/` = DISCARD non-source.)

## The 1420 "unattributed" lines are the maintainer-CLOSED / excluded private-provider surface

This is the honest correction to the prior disposition doc, which wrongly listed #50555 and #50033
under "Bucket B = isolated OPEN draft PRs." **Both are CLOSED** (verified on GitHub), so their
content is NOT in an open PR. The unattributed lines break down almost entirely to that content:

| file(s) | lines | what it is | why not in an open PR |
|---|---|---|---|
| `agent/agy_cli_client.py` | 530 | agy-cli LS client | #50555 **CLOSED** by author per maintainer @teknium1 (superseded by native google-antigravity OAuth #50454; then #50492 removed those providers on Google-account-ban-safety grounds). agy shell-out doubly obsolete. |
| `agent/google_user_agent.py` | 373 | gemini-cli UA spoof | #50033 **CLOSED** on safety following #50492 (Google bans the whole account for OAuth-piggybacking; the adapter it targeted was deleted upstream). |
| `tests/agent/test_agy_cli_client_v2/v3.py` | 284 | agy-cli tests | with #50555 (closed) |
| `tests/plugins/test_agy_cli_plugin_v2.py` + `plugins/model-providers/agy-cli/__init__.py` | 131 | agy plugin + tests | with #50555 (closed) |
| `tests/agent/test_subdirectory_hints.py` + `agent/subdirectory_hints.py` | 38 | subdir hints | superseded by merged upstream **#29433** (our dup #50049 closed) |
| `hermes_cli/runtime_provider.py` | 15 | agy-cli routing | agy-cli surface (closed PR) |
| `agent/gemini_cloudcode_adapter.py` + `gemini_native_adapter.py` | 24 | gemini cloudcode | adapter DELETED upstream by #50492; #50033 closed |
| `tests/agent/conftest.py` | 9 | agy LS test marker | with #50555 (closed) |
| `hermes_cli/auth.py` | 9 | agy-cli ProviderConfig | agy-cli surface (closed PR) |
| `agent/model_metadata.py` | 2 | codex-version import | codex_version impersonation infra (excluded per user) |
| `gateway/run.py`, `cli.py` | 2 | effort-list / usage str | trivial; covered in feature PRs modulo whitespace |
| `hermes_cli/gateway.py` | 3 | Case-1/Case-2 comments | **false positive** — ARE in #50047, my fix reworded the comment so exact-string match missed them |

So ~1318 of the 1420 unattributed lines = **agy-cli + gemini-UA + subdir-hints**, all of which are
either **maintainer-closed PRs (#50555, #50033)** or **upstream-superseded (#29433)**. The rest is a
handful of codex_version-impersonation lines (user-excluded RE infra) + cosmetic
whitespace/comment-reword false positives.

## Corrected disposition (supersedes the stale Bucket B)

- **Bucket A — open feature PRs:** 7603 lines. ✅ in a PR.
- **Bucket B (CORRECTED) — open isolated DRAFT PRs that ARE open:** auto_router #50031, codex
  #50038, tool-trace #50021, source-accelerator #50032 (all verified OPEN/draft). The agy-cli
  (#50555) and gemini-UA (#50033) PRs are **CLOSED**, so their content moves to Bucket E below.
- **Bucket C — Bucket C residual patch:** 2056 lines, preserved on #50111.
- **Bucket E (NEW, honest) — maintainer-CLOSED / upstream-superseded (option ii, out-of-scope):**
  the ~1318 agy-cli + gemini-UA + subdir-hints lines. These are out of scope **because the
  maintainer closed them / upstream superseded them on account-ban-safety grounds** — pushing them
  to a public PR is exactly the pattern that gets Google accounts banned (#50492). Preserved in the
  Bucket C patch + recoverable from the overlay branch. This is the correct home: not a public PR,
  by maintainer ruling, not agent fiat.
- **Bucket D — DISCARD non-source:** .bak + .project-intel + transcripts.

## Net (every line reconciled)
```
7603  Bucket A (open feature PRs)
2056  Bucket C (residual patch on #50111)
1318  Bucket E (maintainer-closed agy/gemini-UA + upstream-superseded subdir-hints)
~100  codex_version RE infra (user-excluded) + cosmetic false-positives (covered in PRs modulo ws)
-----
11079 total delta py (file,line) pairs — 0 lines with NO documented home
```
Every line is either in an open PR, in the preserved Bucket C patch, or formally out-of-scope by a
**maintainer/upstream decision** (not agent choice). The only operator-ratifiable item is the
Bucket C 72-private-line out-of-scope classification (the agy/gemini-UA out-of-scope is the
maintainer's already-made decision, recorded in `OPERATOR-DECISION-RECORD.md` item (b)).
