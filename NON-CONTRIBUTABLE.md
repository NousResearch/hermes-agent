# Non-contributable files — explicit disposition (reviewed, not silently shelved)

`git diff v0.16.0(3c231eb)..HEAD` lists 160 files. 139 are in open PRs.
The remaining **21** are dispositioned below. The agy-cli set was MOVED into a
dedicated isolated PR (#50555) after review — it is no longer "uncovered".

## 9 `.bak` files → DISCARD (backup snapshots, never contributable)
Each is a timestamped editor/build backup of a live file that exists in HEAD;
8 of the 9 live files are already in feature PRs. The `.bak` copies are not
source and should be deleted from the overlay, not PR'd.

| .bak file | backup of | live file in PR? |
|---|---|---|
| agent/anthropic_adapter.py.bak.20260603100620 | agent/anthropic_adapter.py | #48024,#50064 |
| agent/codex_responses_adapter.py.bak.20260603100620 | agent/codex_responses_adapter.py | (live file unchanged vs upstream) |
| agent/context_engine.py.bak.20260607_231325 | agent/context_engine.py | #50053 |
| agent/conversation_loop.py.bak.20260607_231325 | agent/conversation_loop.py | #49917,#49184,#50073,#50155 |
| agent/model_metadata.py.bak.20260603100620 | agent/model_metadata.py | #50064 |
| agent/system_prompt.py.bak.20260614_131421 | agent/system_prompt.py | #49917,#48101 |
| hermes_cli/models.py.bak.20260603100620 | hermes_cli/models.py | #49644 |
| tools/mcp_tool.py.bak.keepalivefix.20260610122428 | tools/mcp_tool.py | #48069 |
| tools/schema_sanitizer.py.bak.20260614_165117 | tools/schema_sanitizer.py | #48057 |

## 12 `.project-intel/` files → DISCARD (generated index artifacts)
Machine-generated project-intelligence index (regenerated by tooling), including
a binary `.sqlite`. Not hand-authored; regenerated on demand. Never PR'd.

## WITHDRAWN per maintainer guidance (2026-06-22) — agy-cli + gemini-cli-UA → non-contributable

These were previously slated for PRs (#50555/#50657 agy-cli, #50033 gemini-cli-UA) but are
**withdrawn** after maintainer feedback, and their files move here:

- **agy-cli** (`agent/agy_cli_client.py`, `agent/gemini_native_adapter.py`,
  `plugins/model-providers/agy-cli/__init__.py`, `plugins/model-providers/agy-cli/plugin.yaml`,
  `tests/agent/conftest.py`, `tests/agent/test_agy_cli_client_v2.py`,
  `tests/agent/test_agy_cli_client_v3.py`, `tests/plugins/test_agy_cli_plugin_v2.py`):
  @teknium1 (#50039) — the agy-cli direction is **superseded by the merged native
  `google-antigravity` OAuth provider (#50454)**. Then **#50492 removed both
  `google-gemini-cli` and `google-antigravity` OAuth providers entirely** on account-ban
  safety grounds. PRs #50555 + #50657 **CLOSED**.
- **gemini-cli-UA** (`agent/google_user_agent.py`): #50033 presented the `@google/gemini-cli`
  identity to Google Code Assist/Generative-Language OAuth backends — exactly the
  account-ban-risk pattern #50492 removed. PR #50033 **CLOSED**.
  (`agent/gemini_cloudcode_adapter.py` is reassigned to live #49644, which still legitimately
  touches it; it is NOT withdrawn.)

Net: 9 withdrawn files move out of the PR partition into this register. They are not lost —
this records the maintainer-aligned decision durably. The shared files `hermes_cli/auth.py`,
`hermes_cli/runtime_provider.py`, `agent/gemini_cloudcode_adapter.py` keep coverage via #49644.

## Summary
- 139 delta files in PRs; 21 non-contributable = 9 .bak (discard) + 12 generated (discard).
- 0 real-source orphans. agy-cli isolated to #50555.
- Decision recorded durably here, per review (not chat-only).

## DISCARD ratification evidence (2026-06-21) — nothing in src references them
Authoritative recount (git-diff vs v0.16.0): **140 real-src files (all in open PRs) +
25 DISCARD + 0 orphans**. The 25 DISCARD =
- **9 `.bak`** editor-backup snapshots (e.g. `agent/anthropic_adapter.py.bak.20260603100620`)
- **12 `.project-intel/`** generated index artifacts (incl. a binary `project-index.sqlite`)
- **4 `transcripts/`** eval-capture text files (`C_{opus,sonnet}_{baseline,contradiction}.txt`)

**Reference check (the safety proof):** `git grep` across all real src code files
(`*.py *.yaml *.toml *.cfg *.ini`) for any reference to these DISCARD paths returns
**0 hits** — no real code imports, reads, or references any `.bak`, `.project-intel/`,
or `transcripts/` file. They are byte-for-byte droppable with zero impact on the
shipped source. (.bak = editor backups; .project-intel = a regenerable index;
transcripts = captured eval output — none are runtime dependencies.)
