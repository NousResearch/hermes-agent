# Authorised local Hermes authority-history amendment

Siba said Ok go after the recommendation to implement the ownership-history fix in a separate Hermes development checkout and test real runtime code. This authorises local edits/tests only. You are an actual Hermes CLI coding worker, NOT BUREAU/CLIENT. Scope is C:/Users/sibag/hermes-phase3-authority-history on feat/phase3-authority-history, base 09109fec98016ffd7fef8622223073d296c02fa4 (same as installed runtime). Do NOT update to latest remote or modify shared main/worktree metadata. No staging, commit, push, merge, deployments, live database/profile/config writes, service restart, schedule, sends or automatic-recovery implementation. Do not modify the independent Bureau worktree.

Read repo AGENTS.md fully and relevant testing instructions. Design input READ ONLY: C:/Users/sibag/duro-bureau-phase3-plan/docs/plans/OWNERSHIP-HISTORY-DESIGN.md. Its proof demonstrates selected extracted functions/minimal schema, not full runtime acceptance. Implement recommended authority-owned same-database append-only journal through normal owned schema migration and existing transaction boundary, not Bureau monkeypatches and not after-commit hooks. Keep generic reusable infrastructure; no Bureau-specific core tool or second scheduler.

Target minimal complete opt-in durable ownership-history capability. Existing boards/ordinary task operations must preserve behavior unless explicitly enrolled. Scope includes supported enrollment/capability and authority incarnation, immutable versioned bindings (repo/project/task, consumer/runtime/non-secret owner reference), strict ordered reader with coverage/highwater, transactional journal capture, retention across gc/task deletion with terminal tombstones, board-removal refusal for enrolled active authority, malformed/unknown state fail-closed. Inspect all lifecycle mutation paths (review claims, claim/renewal/reclaim/complete/block/archive/delete/defensive repair) and provide explicit coverage. Do not promise lossless transitions not emitted. Failed journal writes must rollback authority transitions. No backfill of missing history. Never export bearer tokens in public records. If minimal implementation cannot meet a requirement, report the exact blocker rather than disabling a test or pretending done.

TDD required: capture real failing test, implement, rerun. Tests must use actual imported candidate Hermes code and real disposable sqlite, not extracted AST as sole acceptance. Include concurrent processes exactly one claim, rollback on journal error, abrupt process exit pre/post commit, retained history after gc/delete, stale/unknown sequence/incarnation, immutability, enrollment semantics, default-board compatibility, migration reopen, deletion/removal, and all supported lifecycle coverage. Safe old-worker execution-tree exclusion is independently unresolved and outside this change; do not claim this journal solves it.

All test state strictly local to this worktree. Before importing runtime for tests set/scrub child HERMES_HOME, board paths, HOME/USERPROFILE as required, TEMP/TMP/TMPDIR and cached tempfile resolution. Preserve Windows SYSTEMROOT/WINDIR/COMSPEC. Verify actual tempfile.gettempdir and resolved module path before tests. Do not open live boards or launch task workers/gateways. Existing installed interpreter C:/Users/sibag/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe may be used read-only, with candidate source import path proven. Use existing dependencies first. No global installs. If test dependencies missing, use a local venv only if available without modifying outside paths; otherwise document blocker. Initial baseline clean. Installed source may be read for baseline hashes, never edited.

Write PHASE3-HISTORY-PROGRESS.md early. Store evidence under local .phase3-evidence/ (ignore disposable state but preserve logs). Run focused new tests and adjacent Kanban tests, syntax and diff checks; enumerate real failure counts and skipped tests. Test code must not spawn new LLMs or change memory/skills. Keep local code paths within scope. No duplicate broad implementation agents; do not change authorisation yourself.

On completion or max-turn limit write PHASE3-HISTORY-RESULT.md with complete modified/untracked file manifest, real RED/GREEN logs and commands, interpreter/base, limitations, safety gates and continuation. Independent review is pending parent Hermes; do not self-certify. Verify installed source hashes unchanged. Code exists only in this checkout; no runtime activation implied.
