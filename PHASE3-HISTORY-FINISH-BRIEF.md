# Finish current pass, freeze candidate, no scope expansion

Original local-build authority and constraints remain in PHASE3-HISTORY-BRIEF.md and PHASE3-HISTORY-PASS2.md. Read both and PHASE3-HISTORY-SPEC-REVIEW.md. Previous worker ended at 90-turn limit without final result. This is continuation of current corrections, not new feature scope. Work only C:/Users/sibag/hermes-phase3-authority-history. No live runtime/board/profile writes, external sends, installs, staging/commit/push/merge/activation, real worker launch/PID signalling or automatic recovery. Profiles.py and backup.py candidate source changes are scoped to review's clone/restore refusal, not user profile modifications.

Verified parent checkpoint: git has edits backup.py, profiles.py, kanban_db.py, dashboard plugin, existing board/txn tests, new kanban_history.py and four additional history test files. No PHASE3-HISTORY-PASS2-RESULT.md exists. Previous terminal final report says credential writer8, schema29, clone4, restore10 passed and combined history106 passed at earlier source; adjacent profile21 pass1 fail Windows symlink privilege. Latest .phase3-evidence/pass2-red-owned-insert.log records FOUR genuine RED failures. Owned-insert triggers were then added but administrator-corruption fixture adaptations incomplete and NO GREEN after final code. Progress file stale, verify_pass2.py created but NOT executed. Do NOT treat earlier106 passing tests as final evidence.

Your bounded job: inspect actual last diff/tests, finish owned-insert enforcement plus corruption fixtures without weakening production rules, run strict/history suites through previously reviewed isolated launcher with NEW unique finish-* labels. Preserve genuine REDs and old logs/baselines. Corruption fixtures must model explicit administrator corruption separately, not accidentally open supported bypass. Fix actual failures within this existing slice. Then rerun adjacent safe boards/init/txn/dashboard/profile-copy/backup cases; no broad unsafe full-suite runs. Document Windows symlink privilege failure honestly or use precisely scoped privilege-aware skip with rationale, never hide functional failure. Tests use actual candidate imports, local temp/home/db; no other process modification.

Read run_isolated.py and verify_pass2.py before execution; all runtime tests must go through verified local-state launcher. No global installs. Avoid overwriting prior manifest/evidence: use new finish-* output names or snapshot old files locally first. Run final verification with actual installed3-file hash comparison and candidate manifests, exact source/test path coverage. Save PHASE3-HISTORY-FINISH-RESULT.md EARLY and update as results arrive (not just at tool limit). Include exact command, count/fails/skips, leftover blockers, scopes, API/coverage documentation references and modified files. If source remains red, freeze and report that explicitly. Finish existing verification; no additional architecture/refactors. Independent reviewer needed after you exit; no self-approval.
