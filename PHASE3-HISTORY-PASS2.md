# Pass 2: authorised local corrections after independent spec review

Continue original local-build authority and every boundary in PHASE3-HISTORY-BRIEF.md. Read that brief, PHASE3-HISTORY-RESULT.md and PHASE3-HISTORY-SPEC-REVIEW.md fully. First builder exited; no other editor owns this checkout. You are actual Hermes CLI worker, not BUREAU/CLIENT. Work ONLY C:/Users/sibag/hermes-phase3-authority-history. No live runtime/boards/config/profile edits, installs, commits/staging/push/merge/sends, runtime activation or automatic recovery. Do not touch Bureau worktree or installed source. Existing authorisation covers correcting the requested feature, not scope expansion.

Implement the review's bounded next pass with real behavioral RED/GREEN in priority order:
1. P0 exact final task/run/credential binding and last-snapshot audit plus enforceable owned-writer boundary; coherent autocommit or explicit-BEGIN bypass must refuse on enrolled authority. No second scheduler or administrator-proof claims. Rollbacks include all affected DB rows/history.
2. Strict shared capture/read validation, exact types/strict JSON, relation/provenance consistency, sequence validation, insert/replace immutability even from additional connections; fail closed before new authority if retained stream/capability malformed. Preserve legitimate synthetic unowned terminal runs. Do not silently repair history.
3. Real scheduling/unblocking and bounded lifecycle regression matrix using actual runtime methods; mock only process liveness/termination boundaries, never signal real PIDs or start task workers.
4. Choose review's smaller active-delete REFUSAL for enrolled active tasks; preserve ordinary boards. No pretend deletion means worker death.
5. Document supported single-database authority and prospective task coverage, Python API, runtime instance identity and limitations. Add actual observed source timestamp to journal rather than inventing replay timestamps. Clone/restore identity reuse stays unsupported/UNKNOWN; enforce any existing supported clone/restore/import path, no external fencing system or activation workflow added. Test board removal failure reservation explicitly.

All tests through read/verified .phase3-evidence/run_isolated.py with unique pass2-* labels, actual local TMPDIR/TEMP/TMP/HOME and candidate import proof; never overwrite previous evidence/baselines. Read installed interpreter only. Review source instructions fully. Prior final safe suites listed in RESULT; rerun history, relevant lifecycle/dashboard/boards/init/txn after edits. No unrestricted full repo run if it launches workers or outside writes. Existing self-check verify.py creates manifest; preserve old checkpoint evidence before updating or create pass2 variants.

Keep PHASE3-HISTORY-PASS2-PROGRESS.md current. End with PHASE3-HISTORY-PASS2-RESULT.md (exact changed files, real commands/pass/failure/skips, remaining blockers, source-bound evidence and installed 3-hash comparison). If budget limit approaches, save precise continuation before exiting. No claim complete without all specified gates; independent re-review still required. Do not spend entire budget proposing architecture or broad context gathering: build concrete regression tests and fixes now, retain honest blockers.
