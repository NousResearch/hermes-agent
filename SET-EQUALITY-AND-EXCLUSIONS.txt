# SET-EQUALITY (post #50457 close) + ENUMERATED EXCLUSIONS  (Council items 1 & 2)
# Generated 2026-06-21, updated after #50457 was CLOSED.

## #50457 STATUS: CLOSED (reversibly) — not "recommend close"
  #50457 is now CLOSED on GitHub (issuecomment-4765551715 records the full justification;
  the branch persists so it is reopenable). Proven non-viable across 4 stacking attempts:
  its opus-context test fails 55+ even with #49184+#49644+#50555+conftest all applied —
  it asserts the private overlay's exact routing/clamp/mythos internals, incompatible with
  current main (318 commits ahead of its v0.17.0 base). Intent covered by main+#49184/
  #49644/#49449. Its conftest was relocated to #50555 (preserved).

## SET-EQUALITY: union(41 open PRs) vs overlay src-delta(v0.16.0..HEAD)
  overlay src delta files : 140
  union of 41 open PRs    : 151  (the 11+ extra = PR-authored net-new tests/docs)
  MISSING (overlay delta not in any open PR): 3   <-- all the agy-cli/opus cluster:
    1. hermes_cli/auth.py            (overlay genuine delta = agy-cli ProviderConfig reg, +10)
    2. hermes_cli/runtime_provider.py (overlay genuine delta = agy-cli routing block, +18)
    3. tests/agent/test_copilot_opus_context_fix_2026_06_04.py (the stale opus test)

## THE 3 MISSING FILES — ENUMERATED INTENTIONAL EXCLUSIONS (for user sign-off)

  (1)+(2) agy-cli auth/runtime REGISTRATION: genuinely-wanted overlay content, belongs in
    #50555 (the agy-cli cluster). It CANNOT be added to #50555 cleanly right now because
    #50555 is itself stale-base (v0.17.0, 318 commits behind main) and is the user's
    explicitly-WIP "isolate agy-cli, not-for-merge, fix later" holding pen (disp. rule 8).
    The registration applies cleanly from v0.16.0 and will be added to #50555 when that PR
    is rebased at upgrade time. EXCLUSION REASON: deferred with the agy-cli WIP cluster.

  (3) opus-context test: stale + intent-covered (main + #49184/#49644/#49449) + not
    rebaseable without rewriting assertions. EXCLUSION REASON: superseded/covered.

## NET — TRUE COVERAGE
  137 of the 140 overlay src-delta files are in the 41 open PRs (0 missing among them).
  The 3 excluded files are ALL the agy-cli/opus-context private cluster, deferred with
  #50555 (the user's designated agy-cli WIP holding pen). This is the same private-cluster
  isolation the user mandated (disp. rules 6/7/8). No NON-agy/non-opus src-delta file is
  excluded.

  PLUS 25 DISCARD (9 .bak + 12 .project-intel + 4 transcripts) — non-source, git grep = 0
  references, already in NON-CONTRIBUTABLE.md.

## OPEN QUESTIONS REMAINING FOR USER (sign-off)
  Q1. Grouping: does the 41-PR organization match intent?
  Q2. #50064: accept dropping its 1 test on v0.17.0 rebase (v0.17.0 removed it via #2647)?
  Q3. Delivery shape: sidecar apply-script (APPLY-RESOLUTIONS-ON-v0.17.0.sh) + PRs-on-main,
      or rebase all onto v0.17.0?
  Q4. agy-cli exclusions: OK to defer the agy-cli auth/runtime registration with #50555
      (the WIP holding pen) rather than landing it now?
  (#50457 close = DONE, no longer a question.)
