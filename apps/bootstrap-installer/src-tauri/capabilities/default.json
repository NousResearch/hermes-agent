{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Capabilities for North Forge Setup. Narrowly scoped: a folder picker for choosing the drive/checkout, opening the log folder, and window controls. No arbitrary filesystem or network access from the webview; running bootstrap-north-forge.ps1 happens Rust-side.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-theme",
    "core:event:default",
    "opener:default",
    "dialog:default",
    "process:default",
    "shell:default"
  ]
}
