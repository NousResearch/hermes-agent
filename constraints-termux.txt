# Termux / Android dependency constraints for Hermes Agent.
#
# Usage:
#   python -m pip install -e '.[termux]' -c constraints-termux.txt
#
# These pins keep the tested Android install path stable when upstream packages
# move faster than Termux-compatible wheels / sdists.
#
# The Termux and termux-all extras intentionally omit Hermes' opt-in uvloop
# extra. Uvicorn falls back to Python's stdlib asyncio event loop when uvloop
# is absent, avoiding libuv's Android-incompatible configure step.

ipython<10
jedi>=0.18.1,<0.20
parso>=0.8.4,<0.9
stack-data>=0.6,<0.7
pexpect>4.3,<5
matplotlib-inline>=0.1.7,<0.2
asttokens>=2.1,<3
