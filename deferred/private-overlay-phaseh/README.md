# deferred/private-overlay-phaseh — phase-h private build machinery

**Disposition:** private phase-h overlay build machinery (inventory, copilot catalog
oauth fallback tests, skills_tool overlay). Private infrastructure, not contributable.

**Pull:** `git apply --3way deferred/private-overlay-phaseh/*.patch` for the private
overlay; not for upstream.
