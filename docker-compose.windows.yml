#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host`
#   - Uses explicit port mappings instead
#   - Uses a Windows-compatible bind-mount path for the Hermes data directory
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    build: .
    image: ${DOCKTER_HERMES_IMAGE:-nousresearch/hermes-agent:latest}
    container_name: hermes
    restart: unless-stopped
    volumes:
      - "${DOCKTER_HERMES_DATA_DIR:-${USERPROFILE}/.hermes}:/opt/data"
    environment:
      - HERMES_UID=10000
      - HERMES_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: ${DOCKTER_HERMES_IMAGE:-nousresearch/hermes-agent:latest}
    container_name: hermes-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - "${DOCKTER_HERMES_DATA_DIR:-${USERPROFILE}/.hermes}:/opt/data"
    environment:
      - HERMES_UID=10000
      - HERMES_GID=10000
      - HERMES_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:${DOCKTER_HERMES_DASHBOARD_PORT:-9119}:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open"]
