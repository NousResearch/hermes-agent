# Codex context — balloon placement metadata strategy for fal webtoon lane

## User question
현재 말풍선 위치를 만드는 로직이 어떻게 되어있는지 설명하고,
아래 두 접근 중 무엇이 더 좋은지 **Codex `$ralplan`**으로 계획을 세워라.

Options to evaluate:
1. **Generation-time metadata**: fal 이미지 생성 시점에 말풍선을 어디 넣으라는 메타데이터를 함께 받거나 같이 생성
2. **Post-render VLM analysis**: fal 컷별 결과 이미지에 vision-to-text / VLM을 다시 돌려 말풍선 크기 / 위치 / 타입 메타데이터를 뽑음

Need a recommendation, not a generic brainstorm.

## Current codebase state to ground in
### Current placement logic is NOT generated by fal
The current balloon/caption placement is a **post-render heuristic overlay pipeline**.

Relevant files:
- `docs/plans/orbi-live-webtoon-20260420/webtoon/ep001/analyze_balloon_zones.py`
- `docs/plans/orbi-live-webtoon-20260420/webtoon/ep001/balloon_analysis_ep001.yaml`
- `docs/plans/orbi-live-webtoon-20260420/webtoon/ep001/render_balloons.py`
- `docs/plans/orbi-live-webtoon-20260420/webtoon/ep001/balloon_layout_utils.py`

### What the current analyzer does
`analyze_balloon_zones.py` predefines and scores candidate safe zones plus panel-specific overrides.
Examples seen in code/yaml:
- base safe-zone library such as `speech_top_left`, `speech_top_right`, `speech_upper_mid`, `caption_top_left`, `chat_top_left`, `note_top_right`
- panel-specific forbidden zones like faces, hands, screens, key props
- panel-specific speaker anchors and local zones via:
  - `speaker_anchors`
  - `speaker_local_zones`
  - `item_overrides`
  - `tail_policy`
  - `tail_overrides`
  - `placement_order_policy`
- scoring uses background metrics like luma/stddev/edge activity and overlap checks

### What the renderer does
`render_balloons.py` then:
- resolves speaker anchor with `resolve_anchor(...)`
- computes candidate box size with `measure_box_for_zone(...)`
- optionally computes tail geometry with `build_tail_points(...)`
- draws actual overlay shapes in `render_shape(...)`

Important current behavior:
- current live EP001 renderer keeps tail metadata for scoring/debug but **does not actually render tails** because `should_draw_tail(...)` returns `False`
- so the active lane is already a partially heuristic, partially manually overridden postprocess pipeline

## What the user probably wants
Not just explanation.
A decision on the architecture for the next version of the system:
- whether balloon placement should be pushed upstream into generation metadata
- or stay downstream and become VLM-assisted after render
- and what the best hybrid contract is, if hybrid wins

## Constraints / realities
- fal text-to-image/edit endpoints do not naturally emit comic-layout metadata as first-class structured outputs
- generated image composition can drift, so trusting a pre-generation guess for exact balloon coordinates is risky
- postprocess lettering is already the current contract and works better than asking the model to draw Korean text
- continuity-sensitive webtoon rendering already has enough complexity; do not recommend a solution that couples layout too tightly to image generation unless there is strong reason
- recommendation should distinguish:
  - exact pixel coordinates
  - semantic intent (speaker, importance, preferred side, balloon type)
  - fallback / manual review handling

## What I want from the plan
Produce a bounded planning artifact that answers:
1. current-state explanation of how placement works now
2. evaluation matrix of the two strategies above
3. recommended architecture
4. exact data contract/schema changes needed
5. which files should change
6. how to validate quality
7. a staged rollout plan

## My current hypothesis to pressure-test
Likely best answer:
- **Do not rely on fal to emit exact balloon geometry**
- keep exact placement downstream
- but add a richer intermediate contract:
  - adaptation/generation-time semantic hints (`speaker`, `priority`, `balloon_type`, `preferred_side`, `must_avoid`, `anchor_subject`)
  - optional **post-render VLM pass** that proposes/adjusts candidate zones and speaker anchors from the actual image
- then let the deterministic renderer place final boxes/tails

I want Codex to confirm or reject this with a concrete plan.

## Deliverables required from ralplan
Produce exactly:
- `.omx/context/balloon-metadata-strategy-20260421.md`
- `.omx/plans/prd-balloon-metadata-strategy-20260421.md`
- `.omx/plans/test-spec-balloon-metadata-strategy-20260421.md`

Keep scope to planning only.
Do not implement.
