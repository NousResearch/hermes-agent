# Codex context — Balloon metadata VLM MVP

## Goal
Build a **bounded MVP** for the hybrid balloon-metadata contract using **Codex exec + Codex VLM capability** in the existing romance EP001 live lane.

The user explicitly wants to start with Codex exec because Codex can inspect images.

## Scope
Work only in:
- `/home/orbibot/.zeroclaw/workspace/hermes-agent`
- primarily under `docs/plans/orbi-romance-webtoon-20260421/webtoon/ep001/`

Do not refactor the whole historical live lane.
Do not touch unrelated project areas.

## Current state
Already exists:
- fal-rendered EP001 panels at:
  - `docs/plans/orbi-romance-webtoon-20260421/webtoon/ep001/generated_fal_live_ep001/p01.png` ... `p08.png`
- current live render script:
  - `docs/plans/orbi-romance-webtoon-20260421/webtoon/ep001/render_webtoon_fal_live.py`
- lettering source:
  - `docs/plans/orbi-romance-webtoon-20260421/webtoon/ep001/lettering_script.yaml`
- panel prompts:
  - `docs/plans/orbi-romance-webtoon-20260421/webtoon/ep001/panel_prompts.yaml`
- prior heuristic reference lane:
  - `docs/plans/orbi-live-webtoon-20260420/webtoon/ep001/analyze_balloon_zones.py`
  - `docs/plans/orbi-live-webtoon-20260420/webtoon/ep001/render_balloons.py`
  - `docs/plans/orbi-live-webtoon-20260420/webtoon/ep001/balloon_analysis_ep001.yaml`

## MVP objective
Prove the hybrid contract on one lane with minimal moving parts:
1. Add a **Codex-VLM-produced observation artifact** for EP001 using the already-rendered fal panel PNGs
2. Make the romance lane renderer able to consume that observation artifact for caption/balloon placement
3. Re-render the final EP001 live longscroll using VLM-assisted positions
4. Save debug/provenance output so we can see that the placement came from VLM-assisted observations rather than only fixed hardcoded boxes

## Recommended MVP contract
Add one file:
- `docs/plans/orbi-romance-webtoon-20260421/webtoon/ep001/vlm_observations_ep001.yaml`

Per panel, support fields like:
```yaml
panels:
  - panel_id: p02
    candidate_zones:
      - zone_id: p02_upper_right
        kind: speech
        x: 0.58
        y: 0.05
        w: 0.28
        h: 0.18
        confidence: 0.84
        rationale: clear negative space above heroine shoulder
    anchor_points:
      - anchor_id: heroine_face
        speaker: dialogue
        x: 0.72
        y: 0.34
        confidence: 0.78
    avoid_regions:
      - region_id: laptop_glow
        kind: screen_region
        x: 0.35
        y: 0.50
        w: 0.26
        h: 0.20
        confidence: 0.72
```

Important:
- This is an MVP. Keep it simple.
- Do not attempt full generalized analyzer parity.
- Use Codex image understanding to produce a practical observation file for the actual existing 8 panels.

## Implementation requirements
### 1. Observation artifact generation
Codex should inspect the actual 8 panel PNGs and author a useful `vlm_observations_ep001.yaml`.
This can be hand-authored by Codex after image inspection; it does not need to be generated by a reusable automated script yet.
But the file format should be structured and reusable.

### 2. Renderer upgrade
Update `render_webtoon_fal_live.py` or add a new sibling script if cleaner.
Required behavior:
- load `vlm_observations_ep001.yaml` if present
- for each `panel_id`, use observation-driven placement for captions and balloons
- if no observation exists for a panel/item, cleanly fall back to the current simple fixed placement
- record provenance in manifest, e.g. `placement_source: "vlm_observation" | "fallback_default"`

### 3. Outputs
Keep outputs in the same lane, but preferably separate the VLM-assisted rerender output directory, e.g.:
- `generated_fal_live_ep001_vlm/`
- `generated_fal_live_ep001_vlm/ep001_fal_live_longscroll.png`
- `generated_fal_live_manifest_vlm.json`

### 4. Verification
Verify:
- the observation YAML exists
- updated renderer exits 0
- VLM-assisted longscroll PNG exists
- manifest exists and includes placement provenance
- at least one panel uses `placement_source: vlm_observation`

## Design constraints
- Keep exact final drawing deterministic; do not ask the model to draw Korean text inside the image
- VLM output is advisory, but in this MVP the observation file can directly drive placement rectangles because Codex already inspected the actual images
- Keep the code readable and bounded; this is an MVP for one episode, not the final generic system

## Nice-to-have
- Save a short note file explaining panel-by-panel why each zone was chosen
- Preserve original live render output unchanged so we can compare later

## Deliverables
At minimum produce:
- `docs/plans/orbi-romance-webtoon-20260421/webtoon/ep001/vlm_observations_ep001.yaml`
- updated or new renderer script for VLM-assisted overlay
- VLM-assisted panel outputs + longscroll
- VLM manifest with placement provenance

## Final rule
Do the work now. Do not stop at planning. Implement the MVP and verify it.