# Canonical home decision

This v2 review branch intentionally does **not** add or promote `skills/creative/coach-brand-ingestion/` or `skills/creative/coach-lesson-recap-artifacts/` into the main repo.

Reason: James flagged possible duplicate skill truth. The existing canonical coach-brand lane should remain the authority until James chooses the final in-repo home.

This branch carries:

- the Default Brand Design Document artifact candidate,
- its machine-readable `brand-system-map.json`,
- proof/QA artifacts, and
- review packet documentation describing the desired precedence model.

The guidance is candidate source material for James to merge into the existing canonical coach-brand lane, not a second canonical skill tree.

Precedence model to preserve wherever James installs it:

1. Temporary venue/event Brand Design Document override for the current artifact only.
2. Coach/profile-specific Brand Design Document after brand ingestion.
3. The System Default Brand Design Document fallback for coaches without brand ingestion.
