# Independent Senior Review (Opus)

## Verdict: APPROVE WITH CHANGES

Pass-2 confirms the five pass-1 blockers were folded with *real* gates, not hand-waves — the two-stage spine (§5B) correctly fixes the non-executable-markers problem, the frozen SHA (`929dd9c0d`) is now pinned consistently across §1/§10/Phase 6, the revert-of-merge footgun is explicitly forbidden (§7), and the parked-live-tree question is resolved (OQ-4/Phase 0). I am **not** re-blocking. But two of the five folds (Blocker #1 rename audit, Blocker #5 delta audit) instrument the *wrong artifact* — git metadata instead of the code surface where the failure lives — and Blocker #4's fold is only ~3/7 complete. These are tightenings before execution, not redesigns, hence APPROVE WITH CHANGES rather than APPROVE.

## Critical Blockers (severity-ordered, cite section/evidence)

**None rise to BLOCK.** Prior-blocker verification verdicts (the assigned task):

1. **Pass-1 #1 (clean-merge/rename surface) — RESOLVED-WITH-A-MIS-AIMED-INSTRUMENT.** INV-10 + Phase 1b add the right *idea* (old-absent AND new-present, plus an import-smoke), but the enumeration tool — `git diff -M --diff-filter=R` (§6 Phase 1b, §10 AC-4) — lists only the renames git **detected**. The exact rename-MISS the PRD fears in §1 ("similarity drops below threshold → delete-old + add-new") shows up as **D+A, not R**, so it is *absent from the set the audit walks*. In practice a fork-touched missed-rename surfaces as a modify/delete conflict (git flags it — telegram's rename WAS detected, it's a conflict at the new path), so the silent-outage path is narrower than §1 claims; the genuine residual (clean semantic merge onto relocated context) is caught only by import-smoke (resolution, not behavior) + per-system tests + soak. Net: substantively addressed, instrument needs re-aiming → Required Change, not a re-block.
2. **Pass-1 #2 (non-executable phase gates) — GENUINELY RESOLVED.** §5B's two-stage split (Stage-1 marker-free + import-smoke; pytest only in Stage-2) is mechanically correct and removes the impossible mid-merge "subsystem green" checkpoints. Clean fold.
3. **Pass-1 #3 (config migration vs real fleet config) — RESOLVED, minor mechanics.** Phase 6a dry-run against per-host snapshots with byte-equal key survival + synthetic-key injection is the right gate (see Required Changes for the read-only-vs-writes and interactive-parity nits).
4. **Pass-1 #4 (gates cite tests of unconfirmed existence) — PARTIALLY RESOLVED.** Phase 0a adds verify-then-cite, but (a) INV-1 still cites **coverage areas, not named tests** for 2 of 7 systems — "blackbox cost → `plugins/blackbox/` test coverage" and "kanban → `hermes_cli/kanban_db.py` coverage" — which is exactly the vapor-gate shape #4 named; and (b) revert-to-RED *teeth* are committed for only **"the 3 highest-risk systems"** (Phase 0a E2E bullet) while **INV-1 closeout says "each ... FAILS when the fork behavior is removed."** That is an internal contradiction between the invariant and the phase that implements it.
5. **Pass-1 #5 (un-audited 1,546-commit ingest) — RESOLVED-WITH-A-MIS-AIMED-INSTRUMENT.** INV-9 + Phase 0b are the right gate, but the detection mechanism is **commit-message grep** (`git log --grep='telemetry\|analytics\|HERMES_'`, §6 Phase 0b). A new outbound call or `HERMES_*` read whose commit message doesn't use those words is invisible to it. Awareness-class miss (privacy/posture), not outage-class, so → Required Change.

## Required Changes

- **Re-aim the rename audit at the code surface (folds #1).** Walk upstream **deletes** as well: `git diff --diff-filter=D --name-only c6b0eb4..929dd9c0d` ∩ fork-touched, reconcile each to its new home (or confirm it's a clean upstream-only relocation), *in addition to* the `--diff-filter=R` set. Keep grep as presence-check but state plainly it proves text, not wiring — the behavioral net is the per-system revert-to-RED + soak, not the grep.
- **Re-aim the delta audit at the diff, not the log (folds #5).** Replace/augment the commit-message grep with a code-surface scan of `c6b0eb4..929dd9c0d`: new `os.getenv("HERMES_`/`os.environ[` reads, new `requests.`/`httpx.`/`urllib`/`socket` call sites, and new entries in the tool-registration surface (you already flagged "not just `toolsets.py`" — name the actual registration sites). Message-grep alone under-detects un-labeled additions.
- **Close Blocker #4's fold fully.** Replace the 2 coverage-area citations (blackbox, kanban) with **named test files** verified via `git cat-file -e`, and reconcile the INV-1-vs-Phase-0a contradiction: either commit revert-to-RED for **all 7** INV-1 systems, or amend INV-1 to say only the 3 named systems get mutation-proof and the rest get existence-only (and honestly label the latter "manual verification," per the PRD's own downgrade rule).
- **De-risk the self-restart on host-1 (§6 Phase 6).** Host-1 is defined as "the gateway host carrying Apollo's live session" — deploying + restarting there is a **self-amputation hazard**: a synchronous restart kills the very session orchestrating the deploy and its rollback driver (memory: "NEVER synchronous shutdown/restart → double reboot"; skill `safe-gateway-restart`). Either drive the host-1 restart through `safe-gateway-restart` from a detached context, or pick an equally-representative gateway host that is **not** the deploying session's host. State which.
- **Fix Phase 6a mechanics.** "Read-only snapshot copy" contradicts a `migrate_config` that writes in place — use a **writable** temp copy. And assert the dry-run's `interactive=False` path is the **same code path** the fleet's real first-launch migration takes, or the dry-run validates a branch production won't hit.
- **Confirm the deploy vector before relying on it, with a fallback.** §6 Phase 6 still *hedges* "confirm `fleet.sh deploy` updates core code, not just the bridge." Resolve this in Phase 0/5 as a fact (memory asserts `fleet.sh deploy claude-bridge --restart` ships codebase to all 7), and name the fallback vector if it doesn't.
- **Phase numbering is non-contiguous** (0, 0a, 0b, 1, 1b, 2, 3, **5**, 6a, 6 — no Phase 4). Editing artifact from the fold; renumber so AC/phase cross-refs don't rot.

## Lens Notes (one line each)

- **Architecture:** Strategy and two-stage spine are sound; the residual gap is behavioral-correctness of clean semantic merges, for which import-smoke (resolution-only) leans the load onto a finite soak.
- **Security/identity-isolation:** Secret-scan/SAST/OSV required (INV-4, good); the ingest-audit (INV-9) is the right posture but its commit-message-grep instrument under-detects un-labeled telemetry/env additions.
- **DevOps/SRE:** Per-host SHA rollback + reset-not-revert + staged deploy are now correct; weakened by the host-1 self-restart hazard and a **closed-enumeration** soak threshold that misses fork-system semantic drift not on its 5-signal list.
- **Implementation/maintainability:** Two-stage executability fix is real; non-contiguous phase numbering and the INV-1/Phase-0a contradiction are fold artifacts to clean up.
- **QA:** Strongest area (mutation/revert-to-RED, negative cases), but #4 is only ~3/7 done — 2 systems still cite a directory, not a test, and teeth are proven for 3 of 7.
- **Config-drift:** External drift handled well; Phase 6a now covers the internal migration CI can't see — fix the read-only-vs-writes and interactive-parity mechanics and it's solid.

## Residual Risks / Open Questions

- **Soak threshold is a closed set** (§6 Phase 6: turn-error / `COMPACTION_STATS_RECONCILE_FAILED` / mem0-non-reranked / restart-loop / cron-fail). A semantic clean-merge break in a fork system *outside* that list — blackbox cost miscount, kanban per-task-model silently ignored, send_message routing to the wrong channel in a non-obvious case — passes import-smoke, passes CI (no upstream coverage), and passes soak. This is the surviving silent-regression sliver; the only real net for it is the per-system revert-to-RED tests, which is why closing #4 to 7/7 matters.
- **`--admin` merge (Phase 6/D-6):** PRD correctly states `--admin` doesn't bypass `required_conversation_resolution`, but never says what it *does* bypass (presumably the require-approving-review rule on a sole-maintainer fork). Name it, so "no merge over an open gate" (INV-4) is auditable.
- **`^=======` in the marker grep** (§10) will false-positive on legitimate 7-equals divider lines in docs/docstrings; harmless (fails closed) but expect manual triage on a merge this size.
- **Q:** Does merge-time rename detection (not the audit's `git diff -M`) actually leave any fork-touched file as a *silent clean ADD at the dead path*, or does every such case surface as modify/delete? If the latter (as telegram suggests), §1's headline "clean ADD at dead old path + naive grep passes" threat model is mechanically overstated — worth one empirical check during Phase 1b so the audit targets the real residual (semantic), not a scenario git already self-flags.