# Use Hermes Structure (shortcut · สำเนาในมาตรฐาน เพื่อแจกข้ามเครื่อง)

> ตัวจริงที่ AI โหลด อยู่ที่ Obsidian: `skills/prompt-shortcuts/references/use-hermes-structure.md`
> ไฟล์นี้เก็บไว้ในมาตรฐานเพื่อให้ติดไป git ทุกเครื่อง

คำสั่งเดียวคุมมาตรฐานกลาง Hermes · พิมพ์ "Use Hermes Structure" แล้ว AI โชว์เมนู:

```
1) ใช้/แจก   — safe_apply.py <path> --init  หรือ  hermes_rollout.py --init
2) อัปเดต    — แก้ hermes-standard/rules/central-block.md ที่เดียว → hermes_rollout.py (sync)
3) ตรวจสอบ   — hermes_scan.py + COMPLY.md + curse_track.py report + hermes_analyze.py
4) ทำทั้งหมด — 2 → 1 → 3
```

กฎ: ใช้ safe_apply เสมอ (กันพัง+ถอยกลับ) · แก้กฎกลางที่เดียว · งานบน VPS/~/.claude บอกเจ้าของรันเอง · รันจริง อ่านผลจริง รายงานภาษาคน
