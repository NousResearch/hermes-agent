<!--
Paste into logs/ledger/CHANGELOG.md, newest block on top.
Accumulate CHG- bullets under an "Unreleased" block, then stamp the date when you
cut the version. Rules: logs/ledger/README.md
-->

## [NF-vX.Y.Z] — Unreleased — hermes@<sha> (<N> behind upstream/main)

### Added
- **CHG-YYYY-MM-DD-NNN** — <what was added>. <why>. Paths: `<path>`, `<path>`. Ref: <ERR-/AUDIT- id, or —>.

### Changed
- **CHG-YYYY-MM-DD-NNN** — <what changed>. <why>. Paths: `<path>`. Ref: <…>.

### Fixed
- **CHG-YYYY-MM-DD-NNN** — <bug fixed>. <root cause>. Paths: `<path>`. Ref: ERR-YYYY-MM-DD-NNN.

### Removed
- **CHG-YYYY-MM-DD-NNN** — <what was removed>. <why>. Paths: `<path>`. Ref: <…>.

### Security
- **CHG-YYYY-MM-DD-NNN** — <security-relevant change>. Ref: ERR-YYYY-MM-DD-NNN.

<!--
Version bump (see README):
  MAJOR = incompatible fork reshape (rebrand, subsystem removed/replaced)
  MINOR = new north-forge capability on top of upstream
  PATCH = fixes, housekeeping, upstream sync, docs, ledger-only
-->
