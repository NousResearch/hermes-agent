# Marketing Dan

Static brand reference site for Nervix, Crawbot, Semeclaw, and DansLab.

Files:
- `index.html` — main reference site
- `brand-reference.json` — machine-readable brand data for future agents
- `assets/*.svg` — editable vector brand boards
- `vercel.json` — Vercel static config

Deploy:
`vercel --prod`
