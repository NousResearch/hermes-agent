# hermes-agent-self-evolution
# 此目录由 git clone 自动填充，不要手动提交到 git
# 安装方式：
#   git clone https://github.com/NousResearch/hermes-agent-self-evolution.git
#   cd hermes-agent-self-evolution && pip install -e ".[dev]"
