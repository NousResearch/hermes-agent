// Minimal Typst document — the smallest possible document that compiles.
// Use this as a starting point when the user wants full control over layout
// (e.g. posters, custom typography, single-page handouts).

#set page(margin: 2cm)
#set text(font: "New Computer Modern", size: 11pt)

= Title

Body text.
