# Implementation Plans

Generated by the improve skill on 2026-08-07. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                  | Priority | Effort | Depends on    | Status           |
| ---- | ------------------------------------------------------ | -------- | ------ | ------------- | ---------------- |
| 001  | Formalize GHCR runtime plus voice wiring architecture  | P1       | M      | —             | DONE             |
| 002  | Implement a real TTS fallback chain                    | P1       | M      | 001           | RE-SCOPED by 005 |
| 003  | Stabilize Qwen3-TTS 0.6B model management              | P2       | L      | 001           | RE-SCOPED by 005 |
| 004  | Document voice-provider switching and runtime behavior | P2       | S      | 001, 002, 003 | RE-SCOPED by 005 |
| 005  | Shift voice integration ownership to agent-meow        | P1       | L      | 001           | DONE             |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002 depends on 001 because the fallback chain must target the official GHCR-image-plus-runtime-wiring lane, not an assumed local-build lane.
- 003 depends on 001 because Qwen model management has to fit the official architecture: GHCR containers for Hermes, separate host-side Qwen bridge, mounted runtime config.
- 004 depends on the earlier plans because docs must describe the final chosen runtime and fallback behavior, not the current mixed state.
- 005 is the preferred alternative when the user wants most customization and integration code to live in `agent-meow` rather than Hermes. If 005 is chosen, re-scope 002-004 before executing them as written.

## Findings considered and rejected

- Keep NeuTTS as the Chinese offline fallback: rejected. Current code and model lineup are not Chinese-capable; docs and code should not imply it is a viable Chinese fallback.
- Treat the existing `tts.providers` entries in `data/config.yaml` as an automatic chain: rejected. The current code only selects one provider plus the hardcoded Edge→NeuTTS fallback; configured providers are selectable, not chained.
