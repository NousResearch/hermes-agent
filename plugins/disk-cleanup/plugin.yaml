name: disk-cleanup
version: 2.3.0
description: "Clean only generation-bound files inside fixed Hermes-owned cache and cron roots."
author: "@LVT382009 (original), NousResearch (plugin port)"
hooks:
  - post_tool_call
  - on_session_end
