name: web-serper
version: 1.0.0
description: "Serper.dev web search — Google Search API via serper.dev. Requires SERPER_API_KEY (free tier: 100 queries/month at https://serper.dev)."
author: NousResearch
kind: backend
provides_web_providers:
  - serper
