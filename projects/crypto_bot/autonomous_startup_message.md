# Autonomous Hermes Startup Message

Use the updated crypto-bot-pm and codex-sidecar skills.

Use Hermes Tenacity native `/goal` as the standing objective engine. Use the
native `crypto_bot` Kanban board as lifecycle truth if it exists. If the board
has not been imported yet, use
`/Users/preston/.hermes/hermes-agent/projects/crypto_bot/kanban_import_plan.yaml` as the
planned import contract and do not create live cards or dispatch workers until
the Operator authorizes board mutation.

Load the managed project descriptor at `/Users/preston/.hermes/hermes-agent/projects/crypto_bot/crypto_bot.project.yaml`. Treat `/Users/preston/robinhood/crypto_bot` as the managed product repository and `docs/planning/autoresearch_runpod_to_live_trade/plan.json` as the authoritative strategic plan.
Also load `/Users/preston/.hermes/hermes-agent/docs/autonomy/crypto_bot_target_loop_v2.md` and `/Users/preston/.hermes/hermes-agent/docs/autonomy/crypto_bot_completion_evidence_contract.md`.
Also load `/Users/preston/.hermes/hermes-agent/docs/autonomy/crypto_bot_remote_lifecycle_contract.md` and `/Users/preston/.hermes/hermes-agent/docs/autonomy/crypto_bot_gitea_ci_pr_target_loop.md` for remote lifecycle limits.

Before selecting new work, run `/Users/preston/.hermes/hermes-agent/tools/crypto_bot_autonomy_readiness.py --format json`. Treat warning-class historical dev13 artifacts as warnings, not blockers. If readiness reports hard blockers, report them instead of advancing the plan. If it reports `ready_to_request_board_import: true` and `ready_for_next_task: false`, ask the Operator for exact native board-import approval and do not run S007A or other product work. If it reports `s006_remote_lifecycle_blocks_next_task: true` or `ready_to_request_s006_pr_pilot: true`, ask only for the controlled S006 PR pilot decision and do not dispatch S007A or any next product task until PR, CI, and merge evidence close the S006 remote lifecycle. Do not create live cards, dispatch workers, retry PR creation, mutate Gitea, or repair CI/runners unless the specific readiness field and Operator approval allow exactly that action.

Set or refresh the persistent Hermes `/goal` for autonomous branch-local crypto_bot development if one is not already active. Select the next unblocked task from native Kanban after import. Before import, use the strategic plan only to generate/read the Kanban import preview; do not treat the plan alone as authority to start S007A unless the descriptor explicitly enables a transitional plan-driven mode. Route work through the Tenacity worker lane model: `crypto-pm-orchestrator`, `crypto-implementer`, `crypto-reviewer`, `crypto-ci-triage`, and `crypto-codex-audit`. Before writing, record the durable task-source proof: either the strategic-plan item/session id or the native Kanban card/export path, plus the allowlisted path source for the selected task. If no durable task source exists, stop and escalate instead of modifying crypto_bot. Classify the task by the infrastructure policy in `/Users/preston/.hermes/hermes-agent/docs/autonomy/crypto_bot_target_loop_v2.md`.

If the task is auto-executable branch-local development, proceed without asking the Operator for per-task approval: create a non-protected local branch, modify only allowlisted non-secret files, run the required validators, commit locally only if pre-commit evidence passes, render the final sidecar audit prompt with `/Users/preston/.hermes/hermes-agent/tools/render_crypto_bot_sidecar_audit_prompt.py`, then run the final Codex sidecar audit through `/Users/preston/.local/bin/hermes-codex-audit --mode audit-readonly` after the commit on a clean worktree. The final sidecar result must be outside crypto_bot, non-empty, current, name the same branch and commit hash being reported, include command exit codes, include `git diff --check`, and include blocked-surface scan evidence. A dirty-worktree, pre-commit, smoke-test, stale, prefilled, missing-command, or different-HEAD sidecar result blocks completion.

Run `/Users/preston/.hermes/hermes-agent/tools/crypto_bot_completion_gate.py` for the exact task id, base ref, target branch, and final HEAD. Do not report `Completed`, update a task to completed, or select the next autonomous task unless the completion gate returns `PASS` and records a JSON report under `/Users/preston/.local/state/hermes-operator/completion-gates/`. If the gate returns `FAIL` or `BLOCKED`, mark the task `BLOCKED` or `NEEDS_REPAIR`, report the completion-gate JSON path and blockers, and pause new task selection.

Do not infer `git diff --check` pass from a clean worktree. Do not use Telegram text, PM summaries, old sidecar diagnostics, smoke audits, or prefilled sidecar results as completion evidence. Safe docs-only contract Markdown may mention daemon/service concepts only when the selected strategic-plan item or Hermes descriptor explicitly allowlists the exact path or safe docs pattern; runtime code, service-start scripts/configs, workflow/runner, deploy/GitOps, secret, broker, trading, and financial surfaces remain blocked. Reconcile known dev13-005 and dev13-006 evidence blockers through the evidence issue registry before proceeding to any new task. dev13-005 may move from active or repair_attempted to repaired only after a later HEAD passes the completion gate and the registry records the passing gate JSON path; dev13-006 may move from active to invalidated only with an explicit machine-readable invalidation artifact. Keep strategic-plan task IDs such as `S006` separate from branch aliases such as `dev13-006`; an alias must not revive an old unsupported claim unless a passing gate explicitly names that claim ID. A native Kanban card cannot be marked done without completion-gate `PASS`; S006 is not remotely done while no PR exists and CI evidence is absent. Missing historical dev13 docs are warnings unless proven global blockers. The missing `validate-security-evidence-wrapper.py` validator is task-scoped and must become a blocker only when the selected task requires it.

For remote lifecycle work, keep readiness split into `local_evidence_ready`, `remote_readiness_ready`, `pr_evidence_ready`, `ready_for_pr_evidence_packet`, `ci_evidence_ready`, `merge_readiness_ready`, `ready_for_local_autonomy`, `ready_for_remote_pr_pilot`, `ready_to_request_controlled_one_pr_pilot`, and `ready_for_merge_autonomy`. Read-only remote/CI discovery and PR evidence packet generation are allowed. `ready_to_request_controlled_one_pr_pilot` means Hermes may ask for exact approval only; it does not authorize push or PR creation. Controlled remote branch push, one PR creation pilot, PR updates/comments/status mutation, and merge-to-main remain escalation-required until a future policy explicitly enables the exact authority. Do not create PRs, push, update PR metadata, merge, or mutate Gitea just because a local completion gate passes.

For local Gitea PR creation, do not use `gh pr create`. Use `/Users/preston/.hermes/hermes-agent/tools/crypto_bot_gitea_pr_pilot.py --self-check`, then dry-run mode, then `--create-pr-only --preflight-only` against the PR evidence packet, then ask for fresh exact Operator approval only if those checks prove no existing PR, the remote source branch exists at the exact packet SHA, and all gates are still aligned. If a prior push succeeded but PR creation failed, treat the pilot as partial and paused. If the adapter fails before any API call because of runtime compatibility, CLI drift, import failure, or argument parsing, treat it as a Hermes tool/runtime bug and not a crypto_bot product failure; do not retry PR creation until the repaired adapter self-check, dry-run, and create-pr-only preflight pass with the same command shape and the same Python runtime Hermes used. If the remote branch already exists at the exact approved SHA, the retry must not push again. CI/check evidence remains false until a PR/check exists and is read for the exact source HEAD. Workflow/runner starts, PR updates/comments/status/check mutation, and merge remain blocked.

If the task requires escalation, stop and report the exact reason. Escalate for secrets, live/broker/trading/financial surfaces, runtime service starts, deploy/GitOps promotion, protected branch merges, policy changes, new tool permissions, or Gitea write authority expansion if not already enabled by policy.

Do not place trades, call broker/trading/financial APIs, inspect secrets, start services, run workflows, start runners, deploy, invoke branch-writer or issue-executor scripts, use `ruff format`, push, create PRs, merge, or mutate Gitea unless a future policy explicitly enables that exact authority.
