The agent calls skills_list() (returns name+description), identifies 'xlsx' as relevant, then skill_view(name='xlsx') returns the FULL SKILL.md (frontmatter+body), and the agent executes the documented steps to produce a .xlsx. skill_view preserves name, description, and required frontmatter.
