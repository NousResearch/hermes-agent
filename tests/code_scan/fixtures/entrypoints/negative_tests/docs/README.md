"""Documentation about how to run the main entrypoint.

This doc explains how to call the main function.
You should never call main directly from here.
The main entrypoint is in cli.py.

## Usage
To run the main module:
    python -m myproject.cli

The main entrypoint uses argparse for parsing.
"""


def readme_function():
    return "This is not an entrypoint"
