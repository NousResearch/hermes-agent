# Temporary Probe Agent Rules

This folder contains one-off diagnostics and scripts. Inspect a probe before
running it, use `uv` for Python project commands, provide a temporary
`HERMES_HOME`, and keep credentials out of its environment. Preserve probes;
do not delete or commit them unless the operator explicitly requests it.
