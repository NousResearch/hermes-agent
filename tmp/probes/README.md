# Temporary Probes

Short-lived diagnostics are kept separate from production scripts. Prefer
adding durable behaviour tests under `tests/`; this folder is for local
investigation only and remains ignored.
