# Secret Snapshot Agent Rules

This folder is for local secret-bearing snapshots only. Never print, execute,
commit, upload, or include its contents in reports. Keep permissions restrictive
and use a temporary copy with redacted values for tests or diagnostics.
